{
  "name": "galaid",
  "version": "0.1.0",
  "description": "Local-first launch doctor for visual novel and galgame folders.",
  "main": "desktop/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check src/error-recipes.js && node --check src/app.js && node --check desktop/main.js && node --check desktop/preload.js && node --check desktop/scanner.js"
  },
  "devDependencies": {
    "electron": "^42.0.1"
  }
}
